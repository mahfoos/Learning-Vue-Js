console.log("Hello, vue")  // Vue not use semi colon

const app = Vue.createApp({    // Initialize the app
    // data , function
    // template: '<h2> I am the template </h2>'  // Root component 
})

app.mount('#app') // passing the selector